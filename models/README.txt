Diretório reservado para models usados pelo Sequelize.

"tabela".js - função que retorna model do sequelize para tabela de mesmo nome do BD

index.js - configurações de inicialização do sequelize criadas automaticamente pelo sequelize-cli
via comando "sequelize config:init"